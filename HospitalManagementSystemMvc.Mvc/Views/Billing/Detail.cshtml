@model HospitalManagementSystemMvc.Models.Billing.BillingDetailViewModel
@{
    string title = $"Billing {Model.BillingId}";
    ViewData["Title"] = title;
}

<div class="d-flex justify-content-between align-items-center mb-2">
    <h1>title</h1>
    <a asp-action="Edit" asp-route-id="@Model.BillingId" class="btn btn-light">Edit</a>
    <a asp-controller="Billing" asp-action="Create" asp-route-BillingId="@Model.BillingId" class="btn btn-success">Shop</a>
</div>

<dl class="row">
    <dt class="col-sm-2">
        @Html.DisplayNameFor(model => model.BillingId)
    </dt>
    <dd class="col-sm-10">
        @Html.DisplayFor(model => model.BillingId)
    </dd>
    <dt class="col-sm-2">
        @Html.DisplayNameFor(model => model.PatientId)
    </dt>
    <dd class="col-sm-10">
        @Html.DisplayFor(model => model.Amount)
    </dd>
</dl>

@* <component type="typeof(int)" render-mode="ServerPrerendered" param-transactions="Model.BillingId" /> *@

<div>
    <a asp-action="Index" class="btn btn-primary">Back to List</a>
</div>