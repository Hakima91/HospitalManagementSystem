@model HospitalManagementSystemMvc.Models.Appointment.AppointmentDetailViewModel
@{
    string title = $"Appointment {Model.Id}";
    ViewData["Title"] = title;
}

<div class="d-flex justify-content-between align-items-center mb-2">
    <h1>@title</h1>
    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-light">Edit</a>
    <a asp-controller="Appointment" asp-action="Create" asp-route-Appointment="@Model.Id" class="btn btn-success">Shop</a>
</div>

<dl class="row">
    <dt class="col-sm-2">
        @Html.DisplayNameFor(model => model.Id)
    </dt>
    <dd class="col-sm-10">
        @Html.DisplayFor(model => model.Id)
    </dd>
    <dt class="col-sm-2">
        @Html.DisplayNameFor(model => model.PatientId)
    </dt>
    <dd class="col-sm-10">
        @Html.DisplayFor(model => model.PatientId)
    </dd>
     <dt class="col-sm-2">
        @Html.DisplayNameFor(model => model.DoctorId)
    </dt>
    <dd class="col-sm-10">
        @Html.DisplayFor(model => model.DoctorId)
    </dd>
</dl>

@* <component type="typeof(int)" render-mode="ServerPrerendered" param-Appointment="Model.Id" /> *@

<div>
    <a asp-action="Index" class="btn btn-primary">Back to List</a>
</div>